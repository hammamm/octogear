// Fixture: its path ends in lib/core/storage/app_storage.dart,
// matching the AvoidDirectStorageImports rule's allowlisted caller, so this
// import should NOT be flagged.
import 'package:shared_preferences/shared_preferences.dart';
